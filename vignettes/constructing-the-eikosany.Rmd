---
title: "Constructing the Eikosany"
output: rmarkdown::html_vignette
bibliography: eikosany.bibtex
vignette: >
  %\VignetteIndexEntry{Constructing the Eikosany}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(eikosany)
```

See @narushima2019microtonality for the details. For now we are just running
the examples.

## create_scale_table examples
```{r}

# the defaults yield the 1-3-5-7-9-11 Eikosany
print(eikosany <- create_scale_table())

# the 1-7-9-11-13 Dekany
dekany_harmonics <- c(1, 7, 9, 11, 13)
dekany_choose <- 2
print(dekany_1_7_9_11_13 <- create_scale_table(dekany_harmonics, dekany_choose))

# We might want to print out sheet music for a conventional keyboard
# player, since the synthesizer is mapping MIDI note numbers to pitches.
# We assume at least a 37-key synthesizer with middle C on the left,
# so the largest CPS scale we can play is a 35-note "35-any", made from
# seven harmonics taken three at a time.
harmonics_35 <- c(1, 3, 5, 7, 9, 11, 13)
choose_35 <- 3
print(any_35 <- create_scale_table(harmonics_35, choose_35))
```

## Interval matrix example
```{r}

# the defaults yield the 1-3-5-7-9-11 Eikosany
print(dekany_1_7_9_11_13_interval_matrix <-create_interval_matrix(dekany_1_7_9_11_13))

```

## Tetrads of the eikosany
```{r}

# compute the tetrads of the 1-3-5-7-9-11 Eikosany
print(eikosany_chords <- create_chord_table(eikosany, 4))

```

## Keyboard map examples
```{r}
print(eikosany_map_c4 <- create_keyboard_map(eikosany))
print(eikosany_map_c3 <- create_keyboard_map(eikosany, middle_c_octave = 3))

```


## References
