% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xen_chat.R
\name{xen_chat}
\alias{xen_chat}
\title{xen.assist - Shiny app for xentonal composition via chat}
\usage{
xen_chat(client = NULL, tools = c("env", "run", "files"), messages = list())
}
\arguments{
\item{client}{Configuration for the LLM client; passed through to
\code{btw::btw_client()}. Supply a list or object compatible with your
inference backend (e.g., Ollama). Defaults to \code{NULL}, which may use
whatever defaults \code{btw} resolves.}

\item{tools}{Tools available to the agent. Character vector of tool names,
or list of tool objects. Defaults to \code{c("env", "run", "files")}.}

\item{messages}{Initial message history as a list. Passed through to the
chat module. Defaults to an empty list.}
}
\value{
A Shiny application object (generated by \code{shiny::shinyApp()}).
}
\description{
Launches a two-pane Shiny app: left/main pane runs the \code{btw}-enhanced chat,
right/sidebar pane holds an audio player that plays back the last WAV file
synthesized and a model picker to swap LLM models mid-session.
Uses Bootstrap 5 darkly theme per project conventions.
}
\examples{
\dontrun{
  # Start with default client and standard tools
  xen_chat()
}

}
